# /etc/services.d/20-network.conf — Loopback network interface initialization
name=network
command=/bin/sh -c "ip link set lo up 2>/dev/null || ifconfig lo 127.0.0.1 up 2>/dev/null || true"
after=hostname
restart=never
oneshot=true
critical=false
