# /etc/services.d/10-hostname.conf — Configure system hostname
name=hostname
command=/bin/sh -c "hostname $(cat /etc/hostname 2>/dev/null || echo shreeos)"
after=sysinit
restart=never
oneshot=true
critical=false
