# /etc/services.d/00-sysinit.conf — Early system initialization
name=sysinit
command=/bin/sh -c "echo 0 > /proc/sys/kernel/printk 2>/dev/null || true; mkdir -p /tmp /var/log /var/run /run /var/lib/lpm"
after=
restart=never
oneshot=true
critical=true
