# Update Mechanism

**Status:** scaffolded, implementation lands in Milestone 10+ (see `docs/ROADMAP.md`).

## Purpose

System update tooling built on top of the `lpm` package format, supporting atomic package-level upgrades.

## Layout

_(populated as this milestone is implemented)_

## Building standalone

```bash
# once implemented:
# cd update && make
```

## Testing

Tests for this component live in `tests/` and are also runnable from
within this directory once scripts exist here.
