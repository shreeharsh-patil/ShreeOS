# Branding

**Status:** scaffolded, implementation lands in Milestone 9 (see `docs/ROADMAP.md`).

## Purpose

Distro name, logo, wallpapers, and theme. Everything here is a placeholder driven by `DISTRO_NAME` in `build.conf` — rename the project by changing one variable.

## Layout

_(populated as this milestone is implemented)_

## Building standalone

```bash
# once implemented:
# cd branding && make
```

## Testing

Tests for this component live in `tests/` and are also runnable from
within this directory once scripts exist here.
