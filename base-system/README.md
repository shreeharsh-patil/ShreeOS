# Base System

**Status:** scaffolded, implementation lands in Milestone 3 (see `docs/ROADMAP.md`).

## Purpose

Compiles the core userland (coreutils, bash, util-linux, and friends) against the toolchain from Milestone 2, producing a minimal chroot-able base system.

## Layout

_(populated as this milestone is implemented)_

## Building standalone

```bash
# once implemented:
# cd base-system && make
```

## Testing

Tests for this component live in `tests/` and are also runnable from
within this directory once scripts exist here.
